// import path from 'path';

// export default function(config, env, helpers) {
// console.log(config);
// helpers.getPluginsByName(config,'HtmlWebpackPlugin').forEach(a=>{
//  console.log(a.plugin.options.ssr)});
// console.log(helpers.getLoaders(config));
// const oldBuildPath = env.dest;
// const newBuildPath = path.resolve(env.cwd, 'dist');
// env.dest=newBuildPath;
// config.output.path = config.output.path.replace(oldBuildPath,newBuildPath);
// console.log(config);
// console.log(env);
// console.log(env);
// }
